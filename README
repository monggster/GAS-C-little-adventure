любая программа должна иметь начало и конец.
в линукс это точка входа `_start`
и завершение программы
```c
    exit(<exitcode>);
```
либо
```gas
    movq $60, %rax
    syscall
```

для использования `exit()` нужно подключить стандартную библиотеку `<stdlib.h>`
а для того, чтобы ее использовать, нужно динамически ее слинковать с твоей программой
```sh
ld -o file file.o [file_1.o ... file_n.o] /lib/libc.so -I /lib64/ld-linux-x86-64.so.2
```
-I  -- interpreter = динамичекий линковщик
/lib/libc.so -- динамическая библиотека

можно указать точку входа, отличную от `_start` указав линковщику флаг `-e <entry_point_name>`
```sh
ld -e main -o file file.o
```
-- указание main в качестве точки входа


базовые команды

```
gcc -S file.c -o file.s		# скомпилировать т.е. собрать ассемлерный код из си-кода
as file.s -o file.o		# сассемблировать т.е. собрать объектный файл из ассемблерного
ld file.o -o file		# слинковать т.е. собрать исполняемый файл
```






